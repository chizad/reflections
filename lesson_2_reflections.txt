What happens when you initialize a repository? Why do you need to do it?

A hidden folder called .git is added to my folder. This makes my folder a git repository.


How is the staging area different from the working directory and the repository? What value do you think it offers?

The staging area contains files ready to commit.
The working directory contains all your files.
The repo contains your working directory.